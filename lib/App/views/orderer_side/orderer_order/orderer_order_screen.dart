import 'package:flutter/material.dart';

class OrdererOrderScreen extends StatelessWidget {
  const OrdererOrderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(child: Text('Orderer Order Screen'),),
    );
  }
}