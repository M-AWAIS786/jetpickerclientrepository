import 'package:flutter/material.dart';

class OrdererHomeScreen extends StatelessWidget {
  const OrdererHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(child: Text('Orderer Home Screen'),),
    );
  }
}