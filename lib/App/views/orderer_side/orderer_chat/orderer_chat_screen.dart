import 'package:flutter/material.dart';

class OrdererChatScreen extends StatelessWidget {
  const OrdererChatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(child: Text('Orderer Chat Screen'),),
    );
  }
}